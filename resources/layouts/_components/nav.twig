{% set list = list ?? data.nav %}

<ul class="c-nav {{ class }}">
    {% for item in list %}
        <li class="c-nav__item">
            <a
                href="{{ item.link }}"
                class="c-nav__link"
            >{{ item.title }}</a>
        </li>
    {% endfor %}
</ul>
